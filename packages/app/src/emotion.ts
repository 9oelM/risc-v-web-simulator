/* eslint-disable */
import "@emotion/react"

declare module "@emotion/react" {
  export interface Theme {
    text: string
    warnText: string
    linkText: string
    background: string
    buttonText: string,
    buttonBorder: string,
    buttonBg: string
    buttonBgHover: string
  }
}
